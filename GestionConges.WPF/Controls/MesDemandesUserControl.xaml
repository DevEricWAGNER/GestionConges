<UserControl x:Class="GestionConges.WPF.Controls.MesDemandesUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource Background}">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- En-tête -->
        <Border Grid.Row="0" Style="{StaticResource Card}" 
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Icône et titre -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{StaticResource Primary}" 
                            Width="40" Height="40" CornerRadius="10" 
                            Margin="0,0,16,0">
                        <TextBlock Text="📅" FontSize="20" 
                                 HorizontalAlignment="Center" 
                                 VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Mes Demandes de Congés"
                                 FontSize="20" FontWeight="Bold" 
                                 Foreground="{StaticResource TextPrimary}"/>
                        <TextBlock Text="Gérez toutes vos demandes d'absence" 
                                 FontSize="12" 
                                 Foreground="{StaticResource TextMuted}"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Barre d'actions et filtres -->
        <Border Grid.Row="1" Style="{StaticResource Card}" 
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Bouton nouvelle demande -->
                <Button Grid.Column="0" Name="BtnNouvelleDemande" 
                        Content="Nouvelle Demande"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,20,0"
                        Click="BtnNouvelleDemande_Click">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="➕" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <!-- Filtres -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Filtres :" 
                             FontSize="14" FontWeight="SemiBold"
                             Foreground="{StaticResource TextPrimary}"
                             VerticalAlignment="Center" 
                             Margin="0,0,12,0"/>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Statut" FontSize="12" 
                                 Foreground="{StaticResource TextSecondary}"
                                 VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <ComboBox Name="CmbFiltreStatut" Width="140" Height="32"
                                Background="{StaticResource Surface}"
                                BorderBrush="{StaticResource Gray300}"
                                Margin="0,0,12,0"
                                SelectionChanged="CmbFiltreStatut_SelectionChanged">
                            <ComboBoxItem Content="Tous les statuts" IsSelected="True"/>
                            <ComboBoxItem Content="Brouillons" Tag="Brouillon"/>
                            <ComboBoxItem Content="En attente" Tag="EnAttente"/>
                            <ComboBoxItem Content="Approuvés" Tag="Approuve"/>
                            <ComboBoxItem Content="Refusés" Tag="Refuse"/>
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Année" FontSize="12" 
                                 Foreground="{StaticResource TextSecondary}"
                                 VerticalAlignment="Center" Margin="0,0,4,0"/>
                        <ComboBox Name="CmbFiltreAnnee" Width="100" Height="32"
                                Background="{StaticResource Surface}"
                                BorderBrush="{StaticResource Gray300}"
                                SelectionChanged="CmbFiltreAnnee_SelectionChanged"/>
                    </StackPanel>
                </StackPanel>

                <!-- Bouton actualiser -->
                <Button Grid.Column="2" Name="BtnActualiser" 
                        Content="Actualiser"
                        Style="{StaticResource MaterialDesignOutlineButton}"
                        Click="BtnActualiser_Click">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔄" FontSize="14" Margin="0,0,8,0"/>
                                <TextBlock Text="{Binding}" VerticalAlignment="Center"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>
            </Grid>
        </Border>

        <!-- Liste des demandes -->
        <Border Grid.Row="2" Style="{StaticResource Card}" 
                Padding="0" Margin="0,0,0,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- En-tête de la liste -->
                <Border Grid.Row="0" Background="{StaticResource Gray50}" 
                        Padding="16" BorderBrush="{StaticResource Gray200}" 
                        BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal">
                        <Border Background="{StaticResource Secondary}" 
                                Width="24" Height="24" CornerRadius="6" 
                                Margin="0,0,12,0">
                            <TextBlock Text="📋" FontSize="12" 
                                     HorizontalAlignment="Center" 
                                     VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Liste de vos demandes" 
                                 FontSize="14" FontWeight="SemiBold"
                                 Foreground="{StaticResource TextPrimary}"
                                 VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- DataGrid -->
                <DataGrid Grid.Row="1" Name="DgDemandes" 
                        AutoGenerateColumns="False"
                        IsReadOnly="True"
                        GridLinesVisibility="Horizontal"
                        HeadersVisibility="Column"
                        SelectionMode="Single"
                        Background="Transparent"
                        BorderThickness="0"
                        RowHeaderWidth="0"
                        SelectionChanged="DgDemandes_SelectionChanged"
                        MouseDoubleClick="DgDemandes_MouseDoubleClick">

                    <!-- Menu contextuel -->
                    <DataGrid.ContextMenu>
                        <ContextMenu Name="MenuContextuel" Opened="MenuContextuel_Opened">
                            <MenuItem Name="MenuVoirDetails" Header="👁️ Voir les détails" Click="MenuVoirDetails_Click"/>
                            <Separator/>
                            <MenuItem Name="MenuModifier" Header="✏️ Modifier" Click="MenuModifier_Click"/>
                            <MenuItem Name="MenuDupliquer" Header="📋 Dupliquer" Click="MenuDupliquer_Click"/>
                            <Separator/>
                            <MenuItem Name="MenuSupprimer" Header="🗑️ Supprimer" Click="MenuSupprimer_Click"/>
                        </ContextMenu>
                    </DataGrid.ContextMenu>

                    <!-- Style des en-têtes -->
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="{StaticResource Gray100}"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="12"/>
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="BorderBrush" Value="{StaticResource Gray200}"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>

                    <DataGrid.Columns>
                        <!-- Statut avec couleur -->
                        <DataGridTemplateColumn Header="Statut" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Padding="8,4" CornerRadius="12" Margin="4">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Statut}" Value="Brouillon">
                                                        <Setter Property="Background" Value="{StaticResource Gray200}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Statut}" Value="EnAttenteValidateur">
                                                        <Setter Property="Background" Value="{StaticResource Warning}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Statut}" Value="EnAttenteAdmin">
                                                        <Setter Property="Background" Value="{StaticResource Primary}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Statut}" Value="Approuve">
                                                        <Setter Property="Background" Value="{StaticResource Secondary}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Statut}" Value="Refuse">
                                                        <Setter Property="Background" Value="{StaticResource Danger}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding StatutLibelle}" 
                                                 FontWeight="SemiBold" FontSize="10" 
                                                 Foreground="White"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Type avec couleur -->
                        <DataGridTemplateColumn Header="Type" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="8,0">
                                        <Border Width="16" Height="16" CornerRadius="3"
                                              Background="{Binding TypeAbsence.CouleurHex}" 
                                              Margin="0,0,8,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding TypeAbsence.Nom}" 
                                                 VerticalAlignment="Center"
                                                 FontWeight="Medium"
                                                 Foreground="{StaticResource TextPrimary}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Du" Binding="{Binding DateDebut, StringFormat='dd/MM/yyyy'}" Width="90">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="8,0"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTextColumn Header="Au" Binding="{Binding DateFin, StringFormat='dd/MM/yyyy'}" Width="90">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="8,0"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontWeight" Value="Medium"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="Durée" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="8,0">
                                        <TextBlock Text="{Binding NombreJours}" 
                                                 FontWeight="Bold"
                                                 Foreground="{StaticResource Primary}"/>
                                        <TextBlock Text=" j" 
                                                 Foreground="{StaticResource TextSecondary}"
                                                 Margin="2,0,0,0"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn Header="Commentaire" Width="*" MinWidth="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Commentaire}" 
                                             TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"
                                             Margin="8,0" VerticalAlignment="Center"
                                             Foreground="{StaticResource TextSecondary}"
                                             FontStyle="Italic"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="Créée le" Binding="{Binding DateCreation, StringFormat='dd/MM/yyyy'}" Width="100">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="8,0"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="FontSize" Value="11"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextMuted}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>

                    <!-- Style des lignes -->
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Height" Value="48"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource Gray100}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Statut}" Value="Refuse">
                                    <Setter Property="Opacity" Value="0.7"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Statut}" Value="Brouillon">
                                    <Setter Property="FontStyle" Value="Italic"/>
                                </DataTrigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource Gray50}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource Primary}"/>
                                    <Setter Property="Opacity" Value="0.1"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>

                    <!-- Style des cellules -->
                    <DataGrid.CellStyle>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.CellStyle>
                </DataGrid>
            </Grid>
        </Border>

        <!-- Barre d'actions et statistiques -->
        <Border Grid.Row="3" Style="{StaticResource Card}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Actions sur la demande sélectionnée -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Name="BtnModifier" Content="Modifier"
                            Style="{StaticResource MaterialDesignOutlineButton}"
                            Margin="0,0,8,0" IsEnabled="False" 
                            Click="BtnModifier_Click"/>

                    <Button Name="BtnDupliquer" Content="Dupliquer"
                            Style="{StaticResource MaterialDesignOutlineButton}"
                            Margin="0,0,8,0" IsEnabled="False" 
                            Click="BtnDupliquer_Click"/>

                    <Button Name="BtnSupprimer" Content="Supprimer"
                            Style="{StaticResource DangerButton}"
                            Margin="0,0,16,0" IsEnabled="False" 
                            Click="BtnSupprimer_Click"/>
                </StackPanel>

                <!-- Informations statistiques -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="{StaticResource Gray100}" 
                            CornerRadius="8" Padding="12,6" 
                            Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📊" FontSize="14" Margin="0,0,6,0"/>
                            <TextBlock Name="TxtNombreDemandes" Text="0 demande(s)" 
                                     FontWeight="SemiBold" 
                                     Foreground="{StaticResource TextPrimary}"/>
                        </StackPanel>
                    </Border>

                    <Border Background="{StaticResource Gray100}" 
                            CornerRadius="8" Padding="12,6" 
                            Margin="0,0,12,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📅" FontSize="14" Margin="0,0,6,0"/>
                            <TextBlock Name="TxtTotalJours" Text="0 jour(s) demandé(s)" 
                                     FontWeight="SemiBold"
                                     Foreground="{StaticResource TextPrimary}"/>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="💡 Double-clic pour voir les détails" 
                             FontSize="11" FontStyle="Italic" 
                             Foreground="{StaticResource TextMuted}" 
                             VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>